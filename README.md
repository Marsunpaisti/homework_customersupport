## Installation and setup

Step 1.

```bash
$ npm install
```

Step 2.

Create a .env file based on `example.env` file provided in repository root

## Running the app

```bash
$ npm run db:start
$ npm run db:migrate
$ npm run start
```
