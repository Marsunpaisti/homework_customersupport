import { EntityNotFoundErrorFilter } from './entity-not-found-exception-filter';

describe('EntityNotFoundExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new EntityNotFoundErrorFilter()).toBeDefined();
  });
});
